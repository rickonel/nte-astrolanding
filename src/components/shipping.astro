---
import ContentList from "~/components/content-list.astro";
import ContentSection from "~/components/content-section.astro";
import type { ContentItem } from "~/types";

const pagos: Array<{ title: string }> = [
  { title: "Efectivo (CUP, USD, Euro)" },
  { title: "Transferencia (CUP, MLC, Bancaria)" },
  { title: "Pago por Zelle/Bizum" },
  { title: "Pago Mixto" },
];
---

<ContentSection title="Formas de pago y envío" id="shipping" class="">
  <Fragment slot="lead">
  <div class="mx-auto max-w-5xl text-lg" style="line-height:1.7;">
      NTE <span style="color:var(--color-red);">te acompaña</span> en cada etapa. Elige el suplemento ideal y paga como prefieras, con envíos y atención personalizada. El pago se realizará al momento de la compra, con garantía de que los productos son originales y sellados. Además, realizamos envíos a toda La Habana por un coste adicional.
    </div>
  </Fragment>
  <style>
    #shipping .section-title {
      font-size: 1rem;
    }
    ::selection {
      background: var(--color-red);
      color: white;
    }
  </style>
  <ul class="mx-auto max-w-5xl text-sm mt-8 mb-8 flex flex-row gap-4 justify-center flex-wrap">
    {pagos.map((pago) => (
      <li class="border border-default px-4 py-2 bg-offset text-center font-medium whitespace-nowrap">
        {pago.title}
      </li>
    ))}
  </ul>

  <style>
    #shipping {
      margin-top: 3rem;
      margin-bottom: 3rem;
      padding: 2rem 0;
    }
    #shipping .section-title {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      letter-spacing: 0.02em;
    }
    #shipping .mx-auto {
      padding: 2rem 1rem;
      background: var(--color-white);
      border-radius: 1.5rem;
      box-shadow: 0 2px 24px 0 rgba(0,0,0,0.04);
      margin-left: 1em;
      margin-right: 1em;
    }
    #shipping ul {
      margin: 1.5em 0 1em 2em !important;
      padding-left: 0;
      list-style: none;
      display: flex;
      flex-wrap: wrap;
      gap: 2em;
      justify-content: center;
    }
    #shipping ul li {
      margin-bottom: 1.1em;
      padding: 1.5em 1.5em;
      position: relative;
      font-size: 1.05em;
      background: var(--color-white);
      border-radius: 0;
      box-shadow: 0 2px 12px 0 rgba(0,0,0,0.03);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5em;
    }
    #shipping ul li:before {
      content: '';
      display: inline-block;
      width: 0.5em;
      height: 0.5em;
      background: var(--color-red);
      border-radius: 50%;
      margin-right: 0.7em;
      vertical-align: middle;
    }
    #shipping .shipping-icon {
      width: 40px;
      height: 40px;
      margin-bottom: 0.5em;
      color: var(--color-red);
    }
    #shipping .shipping-img {
      width: 80px;
      height: 80px;
      object-fit: contain;
      margin-bottom: 0.5em;
      border-radius: 1em;
      background: #f8f8f8;
      box-shadow: 0 2px 8px 0 rgba(0,0,0,0.03);
    }
    ::selection {
      background: var(--color-red);
      color: white;
    }
  </style>
