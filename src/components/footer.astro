---
import { Icon } from "astro-icon/components";
import type { FooterLink } from "~/types";

const links: Array<FooterLink> = [
  {
    url: "https://instagram.com/nte_fitness",
    description: "Instagram NTE",
    icon: "mdi:instagram",
  },
  {
    url: "https://wa.me/5352590878",
    description: "WhatsApp NTE",
    icon: "mdi:whatsapp",
  },
  {
    url: "https://chat.whatsapp.com/CVuXGKhFU4zEf6erJCYEbc",
    description: "Grupo WhatsApp NTE",
    icon: "mdi:account-group",
  },
];
---

<footer class="relative flex h-64 items-center justify-center">
  <style>
    @media (max-width: 600px) {
      .section-title, .section-title * {
        font-size: 1em !important;
      }
      .section-content, .section-content * {
        font-size: 0.92em !important;
      }
    }
    @media (max-width: 600px) {
      ul {
        gap: 2rem !important;
      }
      .w-16, .h-16 {
        width: 2.2rem !important;
        height: 2.2rem !important;
      }
      .w-full.text-center {
        font-size: 0.65rem !important;
        line-height: 1.1 !important;
      }
      footer {
        height: 7.5rem !important;
        min-height: 7.5rem !important;
      }
    }
  </style>
  <div class="absolute inset-0 overflow-hidden opacity-40"></div>
  <ul class="relative flex justify-center items-center gap-8">
    {
      links.map((link) => (
        <li>
          <a
            class="flex items-center justify-center"
            href={link.url}
          >
            <span class="sr-only">{link.description}</span>
            <Icon name={link.icon} class="w-16 h-16 text-[var(--color-red)]" />
          </a>
        </li>
      ))
    }
  </ul>
  </ul>
  <div class="w-full text-center mt-1 text-[0.1px] text-[var(--color-red)] absolute bottom-2 left-0">
    © 2025 NTE Fitness. Todos los derechos reservados.<br>
    Desarrollado por Ricardo Onel Alfonso Ayala, CEO de NTE Fitness.
  </div>
</footer>
